# Placeholder Fallbacks

Current fallback rendering is implemented in `CoreCat.tsx` as independent
DOM/SVG layers. These placeholders remain intentionally code-native so missing
PNG/SVG files cannot crash the app.
